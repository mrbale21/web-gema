export interface Training {
  id: number;
  slug?: string | null;
  title: string;
  desc: string;
  icon: string;
}

export interface TrainingDetail {
  id: number;
  slug?: string | null;
  title: string;
  desc: string;
  icon: string;
}

export interface TrainingBenefit {
  id: number;
  slug?: string | null;
  title: string;
}
